name: Hello World

on:
    push:
        branches:
            - dev
    pull_request:
        branches:
            - dev

jobs:
    hello:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - name: hello world
              run: echo "hello world"
    goodbye:
        runs-on: windows-latest
        steps:
            - name: goodbye world
              run: echo "bye world"